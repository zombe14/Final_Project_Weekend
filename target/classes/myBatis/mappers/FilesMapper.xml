<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="FilesMapper">
	<insert id="setWrite" parameterType="FilesDTO">
		<selectKey keyProperty="num" order="BEFORE" resultType="Integer">
			SELECT NOTICE_SEQ.CURRVAL FROM DUAL
		</selectKey>
			INSERT INTO FILES(FNUM, NUM, FNAME, ONAME)
			VALUES (FILES_SEQ.NEXVAL, #{num}, #{FNAME}, #{ONAME})		
	</insert>
</mapper>